import { FC } from 'react';
import { ApiMovie } from '../../models/api/ApiMovie';

interface Props {
  movie: ApiMovie;
}

const Card: FC<Props> = ({ movie }) => {
  return <>Card</>;
};

export default Card;
